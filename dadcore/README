Examples
--------

- to create a pickle of TrackMix objects, use:
PYTHONPATH=`pwd` python examples/trackmix.py [--playlist myplaylist.m3u] tracks.pickle [/path/to/all/my/songs/*]

- to use that pickle to listen to a mix, use:
PYTHONPATH=`pwd` python examples/mix.py tracks.pickle

- to listen to the jukebox:
DAD_DEBUG=4 dad test jukebox --selecter=dad.common.selecter.SimplePlaylistSelecter:--tracks=band.pickle

